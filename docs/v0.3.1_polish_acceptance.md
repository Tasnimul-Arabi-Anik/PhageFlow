# PhageFlow v0.3.1 Polish Acceptance Notes

Date: 2026-07-01

Release tested: `v0.3.0-validated`

## Clean-Room Acceptance Result

The published release was cloned into a fresh temporary directory and tested from the documented parent-directory command context:

```bash
git clone --branch v0.3.0-validated --depth 1 https://github.com/Tasnimul-Arabi-Anik/PhageFlow.git phageflow
bash phageflow/bin/phageflow doctor
bash phageflow/bin/phageflow test
bash phageflow/bin/phageflow run --input phageflow/assets/test_data/toy_phage_a.fasta --pangenome_method none --outdir beginner_single_phage_results
bash phageflow/bin/phageflow validate --outdir beginner_single_phage_results
bash phageflow/bin/phageflow summarize --outdir beginner_single_phage_results --output beginner_single_phage_summary.json
bash phageflow/bin/phageflow package --outdir beginner_single_phage_results --output beginner_single_phage_phageflow_package.tar.gz
bash phageflow/bin/phageflow safety-summary --outdir beginner_single_phage_results --output beginner_single_phage_safety_summary.tsv
bash phageflow/bin/phageflow optional-summary --root beginner_single_phage_results --output beginner_single_phage_optional_tool_summary.tsv
bash phageflow/bin/phageflow structural-summary --outdir beginner_single_phage_results --output beginner_single_phage_structural_summary.tsv
bash phageflow/bin/phageflow run --input phageflow/assets/test_data/phage_samplesheet.tsv --host_samplesheet phageflow/assets/test_data/host_samplesheet.tsv --pangenome_method mmseqs --outdir beginner_cohort_host_results
bash phageflow/bin/phageflow validate --outdir beginner_cohort_host_results --require-pangenome-rows --expect-reference-context
bash phageflow/bin/phageflow summarize --outdir beginner_cohort_host_results --output beginner_cohort_host_summary.json
bash phageflow/bin/phageflow package --outdir beginner_cohort_host_results --output beginner_cohort_host_phageflow_package.tar.gz
```

Outcome:

- First-run `doctor` created the removable `.phageflow-env` environment and reported required tool versions.
- Built-in validation test passed.
- Beginner single-genome run, validation, summary, package, safety summary, optional summary, and structural summary commands passed.
- Beginner cohort run, reference-context validation, summary, and package commands passed.
- Expected report files and key cohort outputs were present.

## v0.3.1 Polish Issue

The README and quickstart say to run commands from the repository root, but many examples use paths such as `phageflow/bin/phageflow`. That works when the current directory is the parent of a clone named `phageflow`, but it fails after the common GitHub workflow:

```bash
git clone https://github.com/Tasnimul-Arabi-Anik/PhageFlow.git
cd PhageFlow
bash phageflow/bin/phageflow doctor
```

Recommended v0.3.1 polish:

- Pick one documented command context and make it explicit.
- Prefer normal clone-root commands in README/quickstart, for example `bash bin/phageflow doctor`, `nextflow run main.nf`, and paths under `assets/test_data/`.
- Keep parent-directory commands only where they are explicitly introduced as parent-directory examples.

No pipeline functionality change is needed for this polish item.
